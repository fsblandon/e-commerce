/* import axios from 'axios';

const massage = (user) => {
    username: user.name;
    password: user.password;
}

export const loginService = (username, password) => {
    axios.get('url')
    .then(
        (respone) => {
            Response
        }
    )
} */
